export enum FORM_TYPE {
  SYSTEM = 'system',
  USER = 'user'
}

export enum FIELD_TYPE {
  INPUT = 'input',
  SELECT = 'select',
  TEXTAREA = 'textarea',
  CHECKBOX = 'checkbox',
  RADIOGROUP = 'radiogroup',
  DATE = 'date'
}
